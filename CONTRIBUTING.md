# 贡献指南

感谢参与中医数字伴侣 Agent 的方案建设。本仓库当前以产品、知识治理、医疗安全和受控验证文档为主。

## 基本流程

1. 从 `main` 创建主题分支。
2. 一次提交只解决一个清晰问题。
3. 通过 Pull Request 提交变更，不直接写入 `main`。
4. 在 PR 中说明变更依据、影响范围、验证方式和未决风险。
5. 至少由知识责任人或医疗安全责任人完成相应审核后再合并。

## 文档要求

- 使用 GitHub Flavored Markdown。
- 仓库内引用使用标准相对 Markdown 链接，不使用 Obsidian `[[双链]]`。
- GitHub Alert 使用大写类型，例如 `> [!WARNING]`。
- 新增专业知识必须记录来源、版本、适用范围、审核状态、责任人和有效期。
- 将事实、访谈证据、产品决策、推导和待验证假设明确区分。
- 修改功能要求时同步更新验收标准、需求追溯和发布阻断项。

## 医疗与隐私红线

禁止提交：

- 可识别患者姓名、电话、身份证号、病历、处方、舌象或检查报告；
- 未完成授权与脱敏的真实案例；
- API 密钥、令牌、密码、生产地址或其他凭据；
- 将 AI 输出描述为医生确认结论的内容；
- 自动诊断、自动处方、自动治疗调整或自动发送医疗意见的实现建议。

示例必须使用虚构或充分脱敏数据，并明确标注“示例”。

## Pull Request 检查

提交者需要完成 PR 模板中的知识来源、医疗边界、隐私、链接和渲染检查。涉及医疗安全、权限、患者数据或知识调用规则的修改，不得由提交者单独批准。
